import "./globals.css";

export const metadata = {
  title: "Base64",
  description: "Convert Base64",
};

export default function RootLayout({ children }) {
  return (

        <html lang="en">
          <body>{children}</body>
        </html>

  );
}
